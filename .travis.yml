language: php
php:
    - '5.6'
    - '7.1'
    - hhvm
    - nightly
before_script
    - composer self-update
    - composer install --prefer-source --no-interaction --dev

script: ./vendor/bin/phpunit --coverage-text --coverage-clover --bootstrap src/autoload.php tests/Container/IocContainerTest.php 
